import { HttpBackend } from "@taquito/http-utils";
import { ContractAbstraction, ContractProvider, Wallet, Context } from "@taquito/taquito";
import { Handler, Tzip16Uri } from "../metadata-provider";
export declare class IpfsHttpHandler implements Handler {
    private _ipfsGateway;
    httpBackend: HttpBackend;
    constructor(ipfsGatheway?: string);
    getMetadata(_contractAbstraction: ContractAbstraction<ContractProvider | Wallet>, { location }: Tzip16Uri, _context: Context): Promise<string>;
}
