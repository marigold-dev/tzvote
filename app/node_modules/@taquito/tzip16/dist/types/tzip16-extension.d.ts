import { Context, Extension } from "@taquito/taquito";
import { Handler, MetadataProviderInterface } from "./metadata-provider";
export declare const DEFAULT_HANDLERS: Map<string, Handler>;
export declare class Tzip16Module implements Extension {
    private _metadataProvider;
    constructor(metadataProvider?: MetadataProviderInterface);
    configureContext(context: Context): void;
}
