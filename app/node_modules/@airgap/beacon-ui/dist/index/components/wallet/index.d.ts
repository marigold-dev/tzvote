import { Component } from 'solid-js';
import styles from './styles.css';
interface WalletProps {
    name: string;
    image: string;
    description?: string;
    small?: boolean;
    mobile?: boolean;
    onClick: () => void;
    tags?: string[];
    disabled?: boolean;
}
declare const Wallet: Component<WalletProps>;
export { styles };
export default Wallet;
